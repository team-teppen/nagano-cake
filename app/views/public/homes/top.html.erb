
<div class="container">
    <div class="row">
        <div class="col-mt-4 pt-md-5">
            <%= render 'public/items/genre_search', genres: @genre %>
        </div>
        <div class="col-lg-7 pt-xl-5">
            <h5 style="text-align: center">
                ようこそ、ながのCAKEへ！<br>
                このサイトは、ケーキ販売のECサイトになります。<br>
                会員ではない方も商品の閲覧はできますが、<br>
                購入には会員登録が必要になります。
            </h5>
        </div>
    </div>
</div>
<div class="container my-5">
    <div class="row">
        <h3 class="px-3 pb-3">新着商品</h3>
    </div>
    <% @items.first(4).each do |item| %>
      <div class="pull-right">
          <%= link_to item_path(item) do %>
            <%= image_tag item.get_image, size: "250x150" %><br>
          <% end %>
          <div class="row">
              <span class="mt-3"><%= item.name %></span>
          </div>
          <div class="row">
              <span class="offset-6">￥<%= (item.price * 1.1).floor %></span>
          </div>
      </div>
      <% end %>
</div>
<div class="container">
    <div class="text-right">
        <%= link_to "全ての商品を見る", items_path %>
    </div>
</div>