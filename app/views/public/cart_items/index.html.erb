<table>
<thead>
  <h2>ショッピングカート</h2>
  <%= flash[:notice] %>
  <tr>
    <th></th>
    <th>商品名</th>
    <th>単価</th>
    <th>数量</th>
    <th>小計</th>
    <th></th>
  </tr>
</thead>
<tbody>



  <% @cart_items.each do |cart_item| %>
  <tr>
    <!--分かりやすくするために記述したので消してokです-->
    <td>[id <%= cart_item.id %>]</td>
    <td>[item_id <%= cart_item.item_id %>]</td>
    <td>[amount <%= cart_item.amount %>]</td>
    <td>[item.id <%= cart_item.item.id %>]</td>
    <!--ここまで-->
    <td><%= cart_item.item.name %></td>
    <td><%= cart_item.item.with_tax_price %></td>
    <td>
      <%= form_with model: cart_item, url: cart_item_path(cart_item.id), method: :patch do |f| %>
        <%= f.select :amount, *[1..10], include_blank: cart_item.amount %>
        <%= f.hidden_field :item_id, :value => cart_item.item.id %>
        <%= f.submit '変更' %>
      <% end %>
    </td>
    <td><%= cart_item.subtotal %></td>
    <td><%= link_to '削除する', cart_item_path(cart_item.id), method: :delete %></td>
    <% @total +=  cart_item.subtotal %>
  </tr>
  <% end %>
合計金額<%= @total %>
</tbody>
</table>
<%= link_to 'カート内を空にする', all_destroy_cart_items_path, method: :delete %>